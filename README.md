# AlgorithmLab
AlgorithmsLab is a C# sandbox for learning algorithms through hands-on implementation. It contains a clean Algorithms library, a Tests project (xUnit) to validate correctness, and an optional Playground console app to experiment with inputs and visualize results..
